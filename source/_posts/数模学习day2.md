---
title: 数模学习day2
date: 2026-01-15 16:34:37
tags:
  - 数模训练
  - 评价模型
categories: 数学建模
math: true
cover: /img/day2.png
---
# Topsis
从现有评价目标中构建出最优目标和最劣目标，计算欧氏距离，得出每个方案与理想化目标的相对接近程度并排序，从而进行结果评价。评价结果的最优解必须是最接近最优解同时最远离最劣解的方案。
## 步骤
1. 原始矩阵正向化：将指标数据转换为极大型指标，指标类型包括极大型指标、极小型指标、中间型指标、区间型指标。

|  指标  |                                                        处理公式                                                        |
| :----: | :--------------------------------------------------------------------------------------------------------------------: |
| 极大型 |                                                           \                                                            |
| 极小型 |                                                    $\hat{x}=max-x$                                                     |
| 中间型 |                          $M=max\{\|x_i-x_{best}\|\}$, $\hat{x}=1-\frac{\|x_i-x_{best}\|}{M}$                           |
| 区间型 | $M=max\{a-x_{min},x_{max}-b\}$, $\hat{x}=1-\frac{a-x_i}{M}\,(x_i<a),\ 1\,(a\le x_i\le b),\ 1-\frac{x_i-b}{M}\,(x_i>b)$ |
2. 矩阵标准化
   $$Z_{ij}=\frac{x_{ij}}{\sqrt{\sum_{i=1}^{n}x_{ij}^2}}$$ 
3. 找出理想最优解、理想最劣解
4. 给指标定权重，层次分析法、熵权法、Delphi法、对数最小二乘法等等
5. 计算各方案与最优解、最劣解的距离进而求出最终得分
   $$
   D_i^+=\sqrt{\sum_{j=1}^{m}{w_j}{(Z_j^+-z_{ij})^2}}
   $$
   
   $$
   D_i^-=\sqrt{\sum_{j=1}^{m}{w_j}{(Z_j^--z_{ij})^2}}
   $$

   $$
   S_i=\frac{D_i^-}{D_i^++D_i^-}
   $$
# 上机操作
在python中的一些用到的函数
```python
n = eval(input()) # 将输入的内容（默认是字符串）转化为整数形式，同时相较于int()可以接收表达式不报错

kind = input().split(" ") # 将输入按照空格分隔
# split('分隔符', 分割次数)

A = np.zeros((n,m)) # 创建一个二维零矩阵，n行m列
# np.zero(shape, dtype=float, order="C")
# 用于创建一个指定 shape 的数组，并将所有元素初始化为零
# 参数说明：
# shape: 数组的形状
# dtype: 数组的数据类型
# order: 数组在内存中的存储顺序，C(按行存储)、F(按列存储)

for i in range(n):
	A[i] = input().split()
	
k	A[i] = list(map(float, A[i])) # 将接收到的字符串列表转换为浮点型
	
# reshape()函数用于改变数组形状
# 当一个数字为-1时，表示该维度的大小由系统自动推导，通过总元素数量和另一个维度来确定新的形状
# reshape(-1, 1)就是把数组变成列向量
arr = np.array([1, 2, 3, 4, 5, 6])
reshaped_arr = arr.reshape(2, 3) 
print(reshaped_arr) # 此时的输出为[[1 2 3] \\ [4 5 6]]

# np.hstack 是NumPy库中的一个函数
# 用于水平方向将多个数组堆叠在一起

# astype(dtyep)函数是将数组的元素类型转换为指定类型，并返回一个新数组

# np.max(array, axis)是numpy库中的函数，用于数组的最大值计算
# axis取0时是列方向的最大值，取1是行方向的最大值，什么都不去默认是求矩阵里面的最大值

# np.tile(A, reps)将数组 A 沿指定方向复制 reps 次，生成一个更大的数组。
# reps: 重复次数，可以是整数或元组，控制沿各个维度的重复次数
```
